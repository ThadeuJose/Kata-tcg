package com.tcg;

import static org.junit.Assert.fail;

import org.junit.Ignore;
import org.junit.Test;

public class PassPlayerTest {

    @Test
    @Ignore
    public void shouldReturnPassMove() {
        // Player player = new ...;
        // Boardstate boardstate= game.createBoardState()
        // Move move = player.chooseMove(boardstate);
        // assertEquals(action.getType(), Type.PASS);
        fail("Not Implement");
    }

}
